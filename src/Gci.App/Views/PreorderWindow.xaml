<Window x:Class="Gci.App.Views.PreorderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Gci.App.ViewModels"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:PreorderViewModel}"
        Style="{StaticResource {x:Type Window}}"
        Title="{Binding Title}" Width="1120" Height="860" MinWidth="720" MinHeight="560"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource Card}" BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="16,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Fill my details" Style="{StaticResource PrimaryButton}" Command="{Binding FillCommand}" />
                    <Button Content="Open in browser" Command="{Binding OpenInBrowserCommand}" Margin="8,0,0,0" />
                </StackPanel>
                <StackPanel Margin="0,0,16,0">
                    <TextBlock FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis">
                        <Run Text="🛒" />
                        <Run Text="{Binding Request.ProductName, Mode=OneWay}" />
                        <Run Text="·" Foreground="{StaticResource MutedBrush}" />
                        <Run Text="{Binding Request.StoreName, Mode=OneWay}" Foreground="{StaticResource MutedBrush}" />
                    </TextBlock>
                    <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" Margin="0,4,0,0" />
                    <TextBlock Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,2,0,0" FontSize="12"
                               Text="GCI puts the item in your bag and fills in your details. You review them and place the order on the page; GCI never submits it." />
                </StackPanel>
            </DockPanel>
        </Border>
        <Border DockPanel.Dock="Top" Background="#FFF6DD" BorderBrush="#F0DDA6" BorderThickness="0,0,0,1" Padding="16,8">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HintText}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding HintText}" TextWrapping="Wrap" />
        </Border>
        <Grid>
            <wv2:WebView2 x:Name="Web" />
            <TextBlock x:Name="Unavailable" Visibility="Collapsed" Margin="32" TextWrapping="Wrap" FontSize="14" />
        </Grid>
    </DockPanel>
</Window>
